import {
  severityClasses
} from "./chunk-SUIC54VS.js";
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger
} from "./chunk-HVYJXDDW.js";
import {
  cn
} from "./chunk-UEVWMCT6.js";

// src/components/vulnerability-chips.tsx
import { jsx, jsxs } from "react/jsx-runtime";
function VulnerabilityDiff({ diffs = 0, className }) {
  return /* @__PURE__ */ jsx(
    "span",
    {
      "data-slot": "vulnerability-diff",
      className: cn(
        "text-xs tabular-nums",
        diffs === 0 && "text-muted-foreground",
        // text-success / text-destructive resolve to AA-passing values on
        // --background in all three modes (light: 6.7:1, dark: 5.6:1,
        // high-contrast: 18:1). The previous high-contrast:text-*-foreground
        // overrides paired with chip backgrounds (red.700/green.700) and
        // failed AA when painted directly on the page bg in HC.
        diffs < 0 && "text-success",
        diffs > 0 && "text-destructive",
        className
      ),
      children: diffs === 0 ? "\u2013" : diffs < 0 ? diffs : `+${diffs}`
    }
  );
}
var SEVERITIES = [
  "critical",
  "high",
  "medium",
  "low",
  "unspecified"
];
function VulnerabilityChips({
  vulnsCount,
  vulnsDiffCount,
  className
}) {
  return /* @__PURE__ */ jsx(
    "div",
    {
      "data-slot": "vulnerability-chips",
      className: cn("inline-flex items-center", className),
      children: SEVERITIES.map((severity, index) => {
        const count = vulnsCount[severity];
        const { bg, text, border } = count === 0 ? severityClasses.none : severityClasses[severity];
        const isFirst = index === 0;
        const isLast = index === SEVERITIES.length - 1;
        return /* @__PURE__ */ jsxs("div", { className: "flex flex-col items-center gap-0.5", children: [
          /* @__PURE__ */ jsxs(Tooltip, { children: [
            /* @__PURE__ */ jsx(TooltipTrigger, { asChild: true, children: /* @__PURE__ */ jsx(
              "span",
              {
                "data-slot": "vulnerability-chip",
                role: "img",
                "aria-label": `${severity.charAt(0).toUpperCase() + severity.slice(1)}: ${count > 999 ? "999+" : count}`,
                className: cn(
                  "min-w-10 px-1.5 py-0.5 text-center text-xs font-label tabular-nums",
                  bg,
                  text,
                  border,
                  isFirst && "rounded-l",
                  isLast && "rounded-r"
                ),
                children: /* @__PURE__ */ jsx("span", { "aria-hidden": "true", children: count > 999 ? "999+" : count })
              }
            ) }),
            /* @__PURE__ */ jsxs(TooltipContent, { side: "top", children: [
              severity.charAt(0).toUpperCase() + severity.slice(1),
              " severity"
            ] })
          ] }),
          vulnsDiffCount && /* @__PURE__ */ jsx(VulnerabilityDiff, { diffs: vulnsDiffCount[severity] })
        ] }, severity);
      })
    }
  );
}

export {
  VulnerabilityDiff,
  VulnerabilityChips
};
