import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

/**
 * A container that groups related content and actions, providing a consistent visual structure.
 *
 * ### Component Props
 *
 * | Prop   | Type                  | Default     |
 * | :----- | :-------------------- | :---------- |
 * | _size_ | `"default" \| "sm"`   | `"default"` |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <Card>
 *   <CardHeader>
 *     <CardTitle>Card Title</CardTitle>
 *     <CardDescription>Card Description</CardDescription>
 *   </CardHeader>
 *   <CardContent>
 *     <p>Card Content</p>
 *   </CardContent>
 *   <CardFooter>
 *     <p>Card Footer</p>
 *   </CardFooter>
 * </Card>
 * ```
 */
declare function Card({ className, size, ...props }: React.ComponentProps<'div'> & {
    size?: 'default' | 'sm';
}): react_jsx_runtime.JSX.Element;
/**
 * The header section of a Card, typically containing a title and description.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <CardHeader>
 *   <CardTitle>Title</CardTitle>
 *   <CardDescription>Description</CardDescription>
 *   <CardAction>
 *     <Button variant="outline">Action</Button>
 *   </CardAction>
 * </CardHeader>
 * ```
 */
declare function CardHeader({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * The title element within a CardHeader.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 */
declare function CardTitle({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * A secondary text element within a CardHeader, used to provide additional context.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 */
declare function CardDescription({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * An action slot within a CardHeader, positioned to the right of the title and description.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 */
declare function CardAction({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * The main content area of a Card.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 */
declare function CardContent({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * The footer section of a Card, typically used for actions or supplementary information.
 *
 * ### Component Props
 *
 * This component does not have any component-specific props.
 */
declare function CardFooter({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * Wraps a Card with 1 or 2 background cards offset behind it, creating a
 * stacked depth effect. Wrap in a sized container to control width.
 *
 * ### Component Props
 *
 * | Prop              | Type       | Default |
 * | :---------------- | :--------- | :------ |
 * | _stackDepth_      | `1 \| 2`   | `1`     |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <CardStack stackDepth={2}>
 *   <CardContent>Content</CardContent>
 * </CardStack>
 * ```
 */
declare function CardStack({ stackDepth, className, children, ...props }: React.ComponentProps<'div'> & {
    stackDepth?: 1 | 2;
}): react_jsx_runtime.JSX.Element;

export { Card, CardAction, CardContent, CardDescription, CardFooter, CardHeader, CardStack, CardTitle };
