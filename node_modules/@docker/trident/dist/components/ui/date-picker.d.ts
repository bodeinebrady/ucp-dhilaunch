import * as react_jsx_runtime from 'react/jsx-runtime';
import { DateRange } from 'react-day-picker';
import { Calendar } from './calendar.js';
import * as React from 'react';
import './button.js';
import 'class-variance-authority/types';
import '../../lib/types.js';
import 'class-variance-authority';

type SharedCalendarProps = Omit<React.ComponentProps<typeof Calendar>, 'mode' | 'selected' | 'onSelect'>;
type DatePickerProps = {
    value?: Date;
    onChange?: (date: Date | undefined) => void;
    placeholder?: string;
    disabled?: boolean;
    className?: string;
    calendarProps?: SharedCalendarProps;
};
/**
 * A single-date picker composed from Calendar + Popover.
 *
 * ### Example Usage
 *
 * ```tsx
 * const [date, setDate] = React.useState<Date | undefined>();
 *
 * <DatePicker value={date} onChange={setDate} placeholder="Pick a date" />
 * ```
 *
 * ### Accessibility (WCAG 2.2 AA)
 *
 * @wcag 1.3.1 Info and Relationships — selected date is exposed via button label.
 * @wcag 2.1.1 Keyboard — calendar grid is keyboard navigable via arrow keys.
 * @wcag 4.1.2 Name, Role, Value — trigger button announces selected date.
 */
declare function DatePicker({ value, onChange, placeholder, disabled, className, calendarProps, }: DatePickerProps): react_jsx_runtime.JSX.Element;
type DateRangePickerProps = {
    value?: DateRange;
    onChange?: (range: DateRange | undefined) => void;
    placeholder?: string;
    disabled?: boolean;
    numberOfMonths?: number;
    className?: string;
    calendarProps?: SharedCalendarProps;
};
/**
 * A date-range picker composed from Calendar + Popover.
 * Selects a start and end date.
 *
 * ### Example Usage
 *
 * ```tsx
 * const [range, setRange] = React.useState<DateRange | undefined>();
 *
 * <DateRangePicker value={range} onChange={setRange} />
 * ```
 */
declare function DateRangePicker({ value, onChange, placeholder, disabled, numberOfMonths, className, calendarProps, }: DateRangePickerProps): react_jsx_runtime.JSX.Element;

export { DatePicker, type DatePickerProps, DateRangePicker, type DateRangePickerProps };
