import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

/**
 * A responsive data table container with horizontal scroll support.
 *
 * ### Component Props
 *
 * Accepts all standard `<table>` element props (`React.ComponentProps<'table'>`).
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <Table>
 *   <TableHeader>
 *     <TableRow>
 *       <TableHead>Name</TableHead>
 *       <TableHead>Status</TableHead>
 *     </TableRow>
 *   </TableHeader>
 *   <TableBody>
 *     <TableRow>
 *       <TableCell>Item 1</TableCell>
 *       <TableCell>Active</TableCell>
 *     </TableRow>
 *   </TableBody>
 * </Table>
 * ```
 */
declare function Table({ className, ...props }: React.ComponentProps<'table'>): react_jsx_runtime.JSX.Element;
/**
 * The header section of a Table, wrapping header rows.
 *
 * ### Component Props
 *
 * Accepts all standard `<thead>` element props (`React.ComponentProps<'thead'>`).
 */
declare function TableHeader({ className, ...props }: React.ComponentProps<'thead'>): react_jsx_runtime.JSX.Element;
/**
 * The body section of a Table, wrapping data rows.
 *
 * ### Component Props
 *
 * Accepts all standard `<tbody>` element props (`React.ComponentProps<'tbody'>`).
 */
declare function TableBody({ className, ...props }: React.ComponentProps<'tbody'>): react_jsx_runtime.JSX.Element;
/**
 * The footer section of a Table, typically used for summary rows.
 *
 * ### Component Props
 *
 * Accepts all standard `<tfoot>` element props (`React.ComponentProps<'tfoot'>`).
 */
declare function TableFooter({ className, ...props }: React.ComponentProps<'tfoot'>): react_jsx_runtime.JSX.Element;
/**
 * A single row within a Table, with hover and selected state styling.
 *
 * ### Component Props
 *
 * Accepts all standard `<tr>` element props (`React.ComponentProps<'tr'>`).
 */
declare function TableRow({ className, ...props }: React.ComponentProps<'tr'>): react_jsx_runtime.JSX.Element;
/**
 * A header cell within a TableRow, used inside TableHeader.
 *
 * ### Component Props
 *
 * Accepts all standard `<th>` element props (`React.ComponentProps<'th'>`).
 */
declare function TableHead({ className, ...props }: React.ComponentProps<'th'>): react_jsx_runtime.JSX.Element;
/**
 * A data cell within a TableRow, used inside TableBody.
 *
 * Defaults to `align-middle` with `whitespace-nowrap` for single-line cells.
 * For multi-line cells (e.g., name + subtitle), add `className="align-top whitespace-normal"`
 * to allow wrapping and top-align all cells in the row with the first line of text.
 *
 * ### Component Props
 *
 * Accepts all standard `<td>` element props (`React.ComponentProps<'td'>`).
 */
declare function TableCell({ className, ...props }: React.ComponentProps<'td'>): react_jsx_runtime.JSX.Element;
/**
 * A caption element for a Table, displayed below the table content.
 *
 * ### Component Props
 *
 * Accepts all standard `<caption>` element props (`React.ComponentProps<'caption'>`).
 */
declare function TableCaption({ className, ...props }: React.ComponentProps<'caption'>): react_jsx_runtime.JSX.Element;

export { Table, TableBody, TableCaption, TableCell, TableFooter, TableHead, TableHeader, TableRow };
