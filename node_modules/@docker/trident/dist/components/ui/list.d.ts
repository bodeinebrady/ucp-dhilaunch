import * as react_jsx_runtime from 'react/jsx-runtime';
import { AsChild } from '../../lib/types.js';
import * as React from 'react';

type ListProps = React.ComponentProps<'ul'> & {
    /** Renders as an ordered list (`<ol>`) when true */
    ordered?: boolean;
};
declare function List({ ordered, className, children, ...props }: ListProps): react_jsx_runtime.JSX.Element;
type ListItemProps = React.ComponentProps<'li'>;
declare function ListItem({ className, ...props }: ListItemProps): react_jsx_runtime.JSX.Element;
type ListItemButtonProps = React.ComponentProps<'button'> & AsChild & {
    /** Marks the item as selected — sets aria-pressed and data-selected */
    selected?: boolean;
};
/**
 * A clickable list row that replaces MUI's `ListItemButton`.
 *
 * Renders as a `<li>` containing a `<button>` by default. Use `asChild` to
 * compose with a router `<Link>` or `<a>` element.
 *
 * ### Component Props
 *
 * | Prop        | Type      | Default |
 * | :---------- | :-------- | :------ |
 * | _selected_  | `boolean` | `false` |
 * | _disabled_  | `boolean` | `false` |
 * | _asChild_   | `boolean` | `false` |
 *
 * ### Example Usage
 *
 * **Basic**
 * ```tsx
 * <List>
 *   <ListItemButton selected>Images</ListItemButton>
 *   <ListItemButton>Containers</ListItemButton>
 * </List>
 * ```
 *
 * **With router link (asChild)**
 * ```tsx
 * <List>
 *   <ListItemButton asChild>
 *     <a href="/images">Images</a>
 *   </ListItemButton>
 * </List>
 * ```
 */
declare function ListItemButton({ className, selected, disabled, asChild, ...props }: ListItemButtonProps): react_jsx_runtime.JSX.Element;

export { List, ListItem, ListItemButton, type ListItemButtonProps, type ListItemProps, type ListProps };
