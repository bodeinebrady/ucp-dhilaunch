import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';
import { Button } from './button.js';
import 'class-variance-authority/types';
import '../../lib/types.js';
import 'class-variance-authority';

/**
 * A navigation container for pagination controls with built-in accessibility attributes.
 *
 * ### Component Props
 *
 * Accepts all standard `<nav>` HTML element props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <Pagination>
 *   <PaginationContent>
 *     <PaginationItem>
 *       <PaginationPrevious href="#" />
 *     </PaginationItem>
 *     <PaginationItem>
 *       <PaginationLink href="#">1</PaginationLink>
 *     </PaginationItem>
 *     <PaginationItem>
 *       <PaginationLink href="#" isActive>2</PaginationLink>
 *     </PaginationItem>
 *     <PaginationItem>
 *       <PaginationEllipsis />
 *     </PaginationItem>
 *     <PaginationItem>
 *       <PaginationNext href="#" />
 *     </PaginationItem>
 *   </PaginationContent>
 * </Pagination>
 * ```
 *
 * ### Accessibility (WCAG 2.2 AA)
 *
 * @wcag 2.1.1 Keyboard — page links are native anchors, natively focusable and activatable via keyboard.
 * @wcag 2.4.4 Link Purpose — PaginationPrevious and PaginationNext carry explicit aria-label; page links convey number via text content.
 * @wcag 4.1.2 Name, Role, Value — renders a `<nav>` element with aria-label="pagination" for landmark identification.
 */
declare function Pagination({ className, ...props }: React.ComponentProps<'nav'>): react_jsx_runtime.JSX.Element;
/**
 * A list container for pagination items that arranges them horizontally.
 *
 * ### Component Props
 *
 * Accepts all standard `<ul>` HTML element props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationContent>
 *   <PaginationItem>
 *     <PaginationLink href="#">1</PaginationLink>
 *   </PaginationItem>
 * </PaginationContent>
 * ```
 */
declare function PaginationContent({ className, ...props }: React.ComponentProps<'ul'>): react_jsx_runtime.JSX.Element;
/**
 * A list item wrapper for individual pagination elements.
 *
 * ### Component Props
 *
 * Accepts all standard `<li>` HTML element props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationItem>
 *   <PaginationLink href="#">1</PaginationLink>
 * </PaginationItem>
 * ```
 */
declare function PaginationItem({ ...props }: React.ComponentProps<'li'>): react_jsx_runtime.JSX.Element;
type PaginationLinkProps = {
    isActive?: boolean;
} & Pick<React.ComponentProps<typeof Button>, 'size'> & React.ComponentProps<'a'>;
/**
 * A pagination link rendered as a button-styled anchor element.
 *
 * ### Component Props
 *
 * Accepts all standard `<a>` HTML element props, plus:
 *
 * | Prop       | Type      | Default  |
 * | :--------- | :-------- | :------- |
 * | _isActive_ | `boolean` | -        |
 * | _size_     | `string`  | `"icon"` |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationLink href="#" isActive>2</PaginationLink>
 * ```
 */
declare function PaginationLink({ className, isActive, size, children, ...props }: PaginationLinkProps): react_jsx_runtime.JSX.Element;
/**
 * A pagination link for navigating to the previous page, with a left chevron icon.
 *
 * ### Component Props
 *
 * Accepts all `PaginationLink` props, plus:
 *
 * | Prop   | Type     | Default      |
 * | :----- | :------- | :----------- |
 * | _text_ | `string` | `"Previous"` |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationPrevious href="#" />
 * ```
 */
declare function PaginationPrevious({ className, text, ...props }: React.ComponentProps<typeof PaginationLink> & {
    text?: string;
}): react_jsx_runtime.JSX.Element;
/**
 * A pagination link for navigating to the next page, with a right chevron icon.
 *
 * ### Component Props
 *
 * Accepts all `PaginationLink` props, plus:
 *
 * | Prop   | Type     | Default  |
 * | :----- | :------- | :------- |
 * | _text_ | `string` | `"Next"` |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationNext href="#" />
 * ```
 */
declare function PaginationNext({ className, text, ...props }: React.ComponentProps<typeof PaginationLink> & {
    text?: string;
}): react_jsx_runtime.JSX.Element;
/**
 * A visual ellipsis indicator representing hidden page numbers in a pagination sequence.
 *
 * ### Component Props
 *
 * Accepts all standard `<span>` HTML element props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <PaginationEllipsis />
 * ```
 */
declare function PaginationEllipsis({ className, ...props }: React.ComponentProps<'span'>): react_jsx_runtime.JSX.Element;

export { Pagination, PaginationContent, PaginationEllipsis, PaginationItem, PaginationLink, PaginationNext, PaginationPrevious };
