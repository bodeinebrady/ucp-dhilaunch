import * as react_jsx_runtime from 'react/jsx-runtime';
import { Button } from './button.js';
import * as React from 'react';
import { DayPicker, DayButton, Locale } from 'react-day-picker';
import 'class-variance-authority/types';
import '../../lib/types.js';
import 'class-variance-authority';

/**
 * A date picker calendar component built on top of `react-day-picker`.
 *
 * ### Component Props
 *
 * See [react-day-picker props](https://daypicker.dev/docs/api/type-aliases/PropsBase) documentation for full details.
 *
 * | Prop               | Type                                                                            | Default     |
 * | :----------------- | :------------------------------------------------------------------------------ | :---------- |
 * | _mode_             | `"single" \| "multiple" \| "range"`                                            | -           |
 * | _selected_         | `Date \| Date[] \| DateRange`                                                  | -           |
 * | _onSelect_         | `(selected: Date \| Date[] \| DateRange \| undefined) => void`                 | -           |
 * | _defaultMonth_     | `Date`                                                                          | -           |
 * | _month_            | `Date`                                                                          | -           |
 * | _onMonthChange_    | `(month: Date) => void`                                                        | -           |
 * | _numberOfMonths_   | `number`                                                                        | `1`         |
 * | _showOutsideDays_  | `boolean`                                                                       | `true`      |
 * | _captionLayout_    | `"label" \| "dropdown" \| "dropdown-months" \| "dropdown-years"`               | `"label"`   |
 * | _disabled_         | `Matcher \| Matcher[]`                                                         | -           |
 * | _locale_           | `Locale`                                                                        | -           |
 * | _showWeekNumber_   | `boolean`                                                                       | `false`     |
 * | _buttonVariant_    | `"default" \| "outline" \| "secondary" \| "ghost" \| "destructive" \| "link"`  | `"ghost"`   |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * const [date, setDate] = React.useState<Date | undefined>(new Date());
 *
 * <Calendar
 *   mode="single"
 *   selected={date}
 *   onSelect={setDate}
 * />
 * ```
 */
declare function Calendar({ className, classNames, showOutsideDays, captionLayout, buttonVariant, locale, formatters, components, ...props }: React.ComponentProps<typeof DayPicker> & {
    buttonVariant?: React.ComponentProps<typeof Button>['variant'];
}): react_jsx_runtime.JSX.Element;
/**
 * A styled button representing a single day cell within the calendar grid.
 *
 * ### Component Props
 *
 * | Prop        | Type              | Default |
 * | :---------- | :---------------- | :------ |
 * | _day_       | `CalendarDay`     | -       |
 * | _modifiers_ | `Modifiers`       | -       |
 * | _locale_    | `Partial<Locale>` | -       |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <CalendarDayButton day={day} modifiers={modifiers} />
 * ```
 */
declare function CalendarDayButton({ className, day, modifiers, locale, ...props }: React.ComponentProps<typeof DayButton> & {
    locale?: Partial<Locale>;
}): react_jsx_runtime.JSX.Element;

export { Calendar, CalendarDayButton };
