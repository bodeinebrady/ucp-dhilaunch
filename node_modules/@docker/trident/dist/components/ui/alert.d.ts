import * as react_jsx_runtime from 'react/jsx-runtime';
import * as class_variance_authority_types from 'class-variance-authority/types';
import * as React from 'react';
import { VariantProps } from 'class-variance-authority';

declare const alertVariants: (props?: ({
    variant?: "default" | "destructive" | "success" | "warning" | "info" | null | undefined;
} & class_variance_authority_types.ClassProp) | undefined) => string;
/**
 * Displays a callout for important messages, warnings, or contextual information.
 *
 * ### Component Props
 *
 * | Prop       | Type                         | Default     |
 * | :--------- | :--------------------------- | :---------- |
 * | _variant_  | `"default" \| "destructive" \| "success" \| "warning" \| "info"` | `"default"` |
 *
 *
 * ### Example Usage
 *
 * **Default**
 * ```tsx
 * <Alert>
 *   <AlertTitle>Heads up!</AlertTitle>
 *   <AlertDescription>
 *     You can add components to your app using the cli.
 *   </AlertDescription>
 * </Alert>
 * ```
 *
 * **Destructive**
 * ```tsx
 * <Alert variant="destructive">
 *   <AlertTitle>Error</AlertTitle>
 *   <AlertDescription>Something went wrong.</AlertDescription>
 * </Alert>
 * ```
 *
 * ### Accessibility (WCAG 2.2 AA)
 *
 * @wcag 4.1.2 Name, Role, Value — uses `role="alert"` to convey importance to assistive technology.
 * @wcag 1.4.3 Contrast (Minimum) — text colors use DDS semantic tokens meeting 4.5:1 contrast.
 */
declare function Alert({ className, variant, ...props }: React.ComponentProps<'div'> & VariantProps<typeof alertVariants>): react_jsx_runtime.JSX.Element;
/**
 * The title of an alert message. Renders as a styled `div`.
 *
 * ### Component Props
 *
 * This component extends the HTML `div` element.
 * It does not have any component-specific props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <AlertTitle>Heads up!</AlertTitle>
 * ```
 */
declare function AlertTitle({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * The description text of an alert message. Renders as a styled `div`.
 *
 * ### Component Props
 *
 * This component extends the HTML `div` element.
 * It does not have any component-specific props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <AlertDescription>
 *   You can add components to your app using the cli.
 * </AlertDescription>
 * ```
 */
declare function AlertDescription({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;
/**
 * A container for an action element positioned at the top-right of an alert.
 *
 * ### Component Props
 *
 * This component extends the HTML `div` element.
 * It does not have any component-specific props.
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <Alert>
 *   <AlertTitle>New update available</AlertTitle>
 *   <AlertAction>
 *     <Button variant="outline" size="sm">Update</Button>
 *   </AlertAction>
 * </Alert>
 * ```
 */
declare function AlertAction({ className, ...props }: React.ComponentProps<'div'>): react_jsx_runtime.JSX.Element;

export { Alert, AlertAction, AlertDescription, AlertTitle };
