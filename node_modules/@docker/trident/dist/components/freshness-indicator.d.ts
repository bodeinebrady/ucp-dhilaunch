import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

type FreshnessBand = 'fresh' | 'aging' | 'stale';
/**
 * Infers a freshness band from a date.
 * - fresh  — < 7 days
 * - aging  — 7–29 days (exclusive upper bound: diffDays < 30)
 * - stale  — ≥ 30 days
 */
declare function inferBand(date: Date | string): FreshnessBand;
type FreshnessIndicatorProps = React.ComponentProps<'span'> & {
    /** ISO date string or Date of last update. */
    date: Date | string;
    /** Override the inferred band. */
    band?: FreshnessBand;
};
declare function FreshnessIndicator({ date, band, className, ...props }: FreshnessIndicatorProps): react_jsx_runtime.JSX.Element;

export { type FreshnessBand, FreshnessIndicator, type FreshnessIndicatorProps, inferBand };
