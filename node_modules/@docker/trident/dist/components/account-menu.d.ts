import * as react_jsx_runtime from 'react/jsx-runtime';
import { PropsWithChildren, ReactNode } from 'react';

type AccountMenuUser = {
    username: string;
    avatarUrl?: string | null;
} | null;
type AccountMenuProps = PropsWithChildren<{
    user: AccountMenuUser;
    chips?: ReactNode[];
    className?: string;
}>;
/**
 * User account avatar trigger + dropdown menu.
 */
declare const AccountMenu: ({ user, chips, children, className, }: AccountMenuProps) => react_jsx_runtime.JSX.Element;

export { AccountMenu, type AccountMenuUser };
