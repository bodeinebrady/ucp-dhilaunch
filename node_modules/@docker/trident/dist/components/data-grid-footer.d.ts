import * as react_jsx_runtime from 'react/jsx-runtime';
import { Table, PaginationState } from '@tanstack/react-table';
import * as React from 'react';

type DataGridFooterProps<TRow extends object> = {
    table: Table<TRow>;
    pagination: boolean;
    rowsPerPageOptions?: number[];
    totalRows: number;
    firstRow: number;
    lastRow: number;
    currentPageSize: number;
    setPaginationState: React.Dispatch<React.SetStateAction<PaginationState>>;
};
declare function DataGridFooter<TRow extends object>({ table, pagination, rowsPerPageOptions, totalRows, firstRow, lastRow, currentPageSize, setPaginationState, }: DataGridFooterProps<TRow>): react_jsx_runtime.JSX.Element;

export { DataGridFooter };
