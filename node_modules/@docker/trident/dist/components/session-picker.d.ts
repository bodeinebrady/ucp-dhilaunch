import * as react_jsx_runtime from 'react/jsx-runtime';

type Session = {
    id: string;
    title: string;
    updatedAt?: string;
    active?: boolean;
};
type SessionPickerProps = {
    sessions: Session[];
    activeId?: string;
    onSelect: (id: string) => void;
    onNew?: () => void;
    className?: string;
};
/**
 * Conversation session switcher for the ConversationLayout header. Shows
 * the active session title; clicking opens a Popover+Command list of all
 * sessions with timestamps. Includes a "New conversation" item at the bottom.
 *
 * Pair with ConversationLayout.Header:
 *
 * @example
 * <ConversationLayout.Header>
 *   <SessionPicker
 *     sessions={sessions}
 *     activeId={activeSessionId}
 *     onSelect={(id) => setActive(id)}
 *     onNew={() => createSession()}
 *   />
 * </ConversationLayout.Header>
 */
declare function SessionPicker({ sessions, activeId, onSelect, onNew, className, }: SessionPickerProps): react_jsx_runtime.JSX.Element;

export { type Session, SessionPicker, type SessionPickerProps };
