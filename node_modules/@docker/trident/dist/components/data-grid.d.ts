import * as react_jsx_runtime from 'react/jsx-runtime';
import { DataGridProps } from './data-grid-types.js';
import 'react';

/**
 * Full-featured data grid built on TanStack Table v8 with DDS styling.
 *
 * @wcag 1.3.1 — semantic `<table>` with `<th>` column headers
 * @wcag 2.1.1 — sort, checkboxes, pagination, expand buttons are keyboard-accessible
 * @wcag 4.1.2 — sort buttons have aria-label; checkboxes have aria-label
 */
declare function DataGrid<TRow extends object = object>({ rows, columns, loading, pagination, pageSize, rowsPerPageOptions, rowCount, onRowClick, checkboxSelection, disableRowSelectionOnClick, sortingMode, onSortModelChange, getRowId, slots, slotProps, density, disableColumnMenu, disableColumnResize, disableColumnReorder, pinnedColumns, rowGroupingModel, onRowGroupingModelChange, columnVisibilityModel, onColumnVisibilityModelChange, processRowUpdate, onProcessRowUpdateError, apiRef, hideFooter, autoHeight, className, filterModel: filterModelProp, onFilterModelChange, disableColumnFilter, treeData, getTreeDataPath, 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledby, onRowSelectionChange, }: DataGridProps<TRow>): react_jsx_runtime.JSX.Element;

export { DataGrid };
