import * as react_jsx_runtime from 'react/jsx-runtime';

declare const TOPOLOGY_NODE_W = 140;
declare const TOPOLOGY_NODE_H = 88;
type TopoMode = 'local' | 'remote';
type BadgeTone = 'ok' | 'err' | 'warn' | 'loading';
type TopologyNodeProps = {
    label: string;
    mode: TopoMode;
    endpoint?: string;
    badge?: string;
    badgeTone?: BadgeTone;
    selected?: boolean;
    requestsPerSec?: number;
    latencyMs?: number;
    secretCount?: number;
    hasDrilldown?: boolean;
    extra?: string;
    onClick?: () => void;
    onDoubleClick?: () => void;
    onToggleMode?: (next: TopoMode) => void;
    className?: string;
};
/**
 * 140x88px interactive card for dependency graph nodes. Renders inside a
 * pan/zoom SVG canvas via a foreignObject. All nodes share the same surface
 * material; local vs remote is differentiated by the Home/Cloud icon.
 *
 * The mode toggle is rendered outside the card button so buttons never nest.
 *
 * @example
 * <TopologyNode
 *   label="app-ui"
 *   mode="local"
 *   endpoint="localhost:3000"
 *   badge="up"
 *   badgeTone="ok"
 *   selected={false}
 *   cpu="6.2%"
 *   memory="235 MB"
 *   onClick={() => select(node)}
 *   onToggleMode={(next) => setMode(next)}
 * />
 */
declare function TopologyNode({ label, mode, endpoint, badge, badgeTone, selected, requestsPerSec, latencyMs, secretCount, hasDrilldown, extra, onClick, onDoubleClick, onToggleMode, className, }: TopologyNodeProps): react_jsx_runtime.JSX.Element;

export { type BadgeTone, TOPOLOGY_NODE_H, TOPOLOGY_NODE_W, type TopoMode, TopologyNode, type TopologyNodeProps };
