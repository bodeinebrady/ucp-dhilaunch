import { DataGridColumn, CsvExportOptions } from './data-grid-types.js';
import { ColumnPinningState, ColumnDef } from '@tanstack/react-table';
import 'react';

/** @internal */
/** Merge column-level pin props with the pinnedColumns prop into a TanStack ColumnPinningState. */
declare function buildColumnPinning<TRow extends object>(columns: DataGridColumn<TRow>[], pinnedColumns?: {
    left?: string[];
    right?: string[];
}): ColumnPinningState;
/** Convert an unknown cell/field value to a display string without hitting [object Object]. */
declare function unknownToStr(v: unknown): string;
declare function toColumnDef<TRow extends object>(col: DataGridColumn<TRow>): ColumnDef<TRow>;
declare function exportToCsv<TRow extends object>(columns: DataGridColumn<TRow>[], rows: TRow[], options?: CsvExportOptions): void;

export { buildColumnPinning, exportToCsv, toColumnDef, unknownToStr };
