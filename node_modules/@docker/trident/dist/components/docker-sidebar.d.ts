import * as react_jsx_runtime from 'react/jsx-runtime';
import { PropsWithChildren, ReactNode } from 'react';

type DockerSidebarProps = {
    /** The sidebar content, rendered as a persistent side panel. */
    sidebar?: ReactNode;
    /** The main page content, rendered alongside the sidebar. */
    children?: ReactNode;
    defaultOpen?: boolean;
    keyboardShortcut?: string | null;
    persistState?: boolean;
};
/**
 * A thin wrapper around `SidebarProvider` that adds Docker-specific opinions:
 * - Cmd+B / Ctrl+B keyboard shortcut to toggle the sidebar
 * - Cookie persistence of the open/closed state across page loads
 */
declare const DockerSidebar: ({ defaultOpen, keyboardShortcut, persistState, sidebar, children, }: DockerSidebarProps) => react_jsx_runtime.JSX.Element;
type AppSidebarProps = PropsWithChildren<{
    header?: ReactNode;
    footer?: ReactNode;
    className?: string;
    variant?: 'sidebar' | 'floating' | 'inset';
}>;
/**
 * Docker app sidebar shell.
 * Persistent left panel, collapsible to icon-only via SidebarRail.
 * Must be a descendant of DockerSidebar (which provides SidebarProvider context).
 * Usage:
 * ```tsx
 * <DockerSidebar sidebar={<AppSidebar>...</AppSidebar>}>
 *   <main>page content</main>
 * </DockerSidebar>
 * ```
 */
declare const AppSidebar: ({ children, header, footer, className, variant, }: AppSidebarProps) => react_jsx_runtime.JSX.Element;
type AppSidebarItemProps = PropsWithChildren<{
    icon?: ReactNode;
    detail?: ReactNode;
    label: string;
    href?: string;
    isActive?: boolean;
    className?: string;
}>;
/**
 * Navigation item for use inside AppSidebar.
 * Leaf: pass href for a link, omit for a button.
 * Parent: pass children (AppSidebarSubItem instances) for a collapsible group.
 */
declare const AppSidebarItem: ({ icon, label, detail, href, isActive, children, className, }: AppSidebarItemProps) => react_jsx_runtime.JSX.Element;
/**
 * Sub-item for nested navigation inside AppSidebarItem parent.
 */
declare const AppSidebarSubItem: ({ label, href, isActive, className, }: Omit<AppSidebarItemProps, "icon" | "children">) => react_jsx_runtime.JSX.Element;

export { AppSidebar, AppSidebarItem, type AppSidebarItemProps, type AppSidebarProps, AppSidebarSubItem, DockerSidebar, type DockerSidebarProps };
