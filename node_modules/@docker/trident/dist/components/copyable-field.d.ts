import * as react_jsx_runtime from 'react/jsx-runtime';

type CopyableFieldProps = {
    /** The text value to display and copy */
    value: string;
    /** Accessible label for the field */
    label: string;
    /** Hide the label visually (still accessible) @default false */
    hideLabel?: boolean;
    /** Callback fired after successful copy */
    onCopy?: () => void;
    className?: string;
};
declare function CopyableField({ value, label, hideLabel, onCopy, className, }: CopyableFieldProps): react_jsx_runtime.JSX.Element;

export { CopyableField, type CopyableFieldProps };
