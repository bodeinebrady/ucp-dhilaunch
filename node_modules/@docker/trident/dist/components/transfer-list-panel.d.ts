import * as react_jsx_runtime from 'react/jsx-runtime';
import { TransferItem } from './transfer-list.js';
import * as React from 'react';

type TransferListPanelProps = {
    label: string;
    description?: string;
    items: TransferItem[];
    checkedIds: ReadonlySet<string>;
    onCheckedChange: (id: string, checked: boolean) => void;
    onSelectAll: (checked: boolean) => void;
    searchable: boolean;
    searchPlaceholder: string;
    searchQuery: string;
    onSearchChange: (value: string) => void;
    renderItem?: (item: TransferItem, side: 'available' | 'selected') => React.ReactNode;
    side: 'available' | 'selected';
    disabled?: boolean;
    filteredItems: TransferItem[];
};
declare const TransferListPanel: ({ label, description, items, checkedIds, onCheckedChange, onSelectAll, searchable, searchPlaceholder, searchQuery, onSearchChange, renderItem, side, disabled, filteredItems, }: TransferListPanelProps) => react_jsx_runtime.JSX.Element;

export { TransferListPanel, type TransferListPanelProps };
