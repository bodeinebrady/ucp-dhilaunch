import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

type TransferItem = {
    id: string;
    label: string;
    description?: string;
    disabled?: boolean;
};
type TransferListProps = {
    available: TransferItem[];
    selected: TransferItem[];
    onChange: (selected: TransferItem[]) => void;
    availableLabel?: string;
    selectedLabel?: string;
    availableDescription?: string;
    selectedDescription?: string;
    searchable?: boolean;
    searchPlaceholder?: string;
    showMoveAll?: boolean;
    renderItem?: (item: TransferItem, side: 'available' | 'selected') => React.ReactNode;
    disabled?: boolean;
    className?: string;
};
declare const TransferList: ({ available, selected, onChange, availableLabel, selectedLabel, availableDescription, selectedDescription, searchable, searchPlaceholder, showMoveAll, renderItem, disabled, className, }: TransferListProps) => react_jsx_runtime.JSX.Element;

export { type TransferItem, TransferList, type TransferListProps };
