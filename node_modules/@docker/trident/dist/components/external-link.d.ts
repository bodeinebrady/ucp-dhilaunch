import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';
import { Link } from './link.js';
import 'class-variance-authority/types';
import '../lib/types.js';
import 'class-variance-authority';

type ExternalLinkProps = Omit<React.ComponentProps<typeof Link>, 'target' | 'rel'> & {
    /** Show external link icon. @default true */
    showIcon?: boolean;
    /** Tooltip text shown on hover. If omitted and `title` is set, `title` is used. */
    tooltip?: string;
    /** Custom tooltip content. Falls back to `href` when provided without `tooltip`. */
    title?: string;
    tooltipPlacement?: 'top' | 'bottom' | 'left' | 'right';
};
declare function ExternalLink({ className, children, showIcon, tooltip, title, tooltipPlacement, href, onAuxClick, onClick, ...props }: ExternalLinkProps): react_jsx_runtime.JSX.Element;

export { ExternalLink, type ExternalLinkProps };
