import * as react_jsx_runtime from 'react/jsx-runtime';

type TimeInputProps = {
    value: {
        hours: number;
        minutes: number;
    };
    onChange: (time: {
        hours: number;
        minutes: number;
    }) => void;
    label?: string;
    className?: string;
};
declare function TimeInput({ value, onChange, label, className }: TimeInputProps): react_jsx_runtime.JSX.Element;
type DateTimePickerProps = {
    /** The selected date and time. */
    value?: Date;
    /** Called when the date or time changes. */
    onChange?: (date: Date | undefined) => void;
    /** Minimum selectable date. */
    minDate?: Date;
    /** Maximum selectable date. */
    maxDate?: Date;
    className?: string;
};
/**
 * A date and time picker combining Calendar with time inputs.
 * Replaces `@mui/x-date-pickers-pro/DateTimeRangePicker` for single datetime selection.
 *
 * ### Component Props
 *
 * | Prop        | Type                              | Default |
 * | :---------- | :-------------------------------- | :------ |
 * | _value_     | `Date`                            | -       |
 * | _onChange_   | `(date: Date \| undefined) => void` | -    |
 * | _minDate_   | `Date`                            | -       |
 * | _maxDate_   | `Date`                            | -       |
 *
 * ### Example Usage
 *
 * ```tsx
 * <DateTimePicker
 *   value={date}
 *   onChange={setDate}
 * />
 * ```
 *
 * ### Accessibility (WCAG 2.2 AA)
 *
 * @wcag 1.3.1 Info and Relationships — time inputs have associated labels.
 * @wcag 2.1.1 Keyboard — Calendar and time inputs are fully keyboard navigable.
 */
declare function DateTimePicker({ value, onChange, minDate, maxDate, className, }: DateTimePickerProps): react_jsx_runtime.JSX.Element;
type DateTimeRange = {
    from?: Date;
    to?: Date;
};
type DateTimeRangePickerProps = {
    /** The selected date/time range. */
    value?: DateTimeRange;
    /** Called when the range changes. */
    onChange?: (range: DateTimeRange) => void;
    className?: string;
};
/**
 * A date-time range picker with two calendars and time inputs.
 * Replaces `@mui/x-date-pickers-pro/DateTimeRangePicker`.
 *
 * ### Component Props
 *
 * | Prop       | Type                                        | Default |
 * | :--------- | :------------------------------------------ | :------ |
 * | _value_    | `{ from?: Date; to?: Date }`                | -       |
 * | _onChange_  | `(range: DateTimeRange) => void`            | -       |
 *
 * ### Example Usage
 *
 * ```tsx
 * <DateTimeRangePicker
 *   value={range}
 *   onChange={setRange}
 * />
 * ```
 */
declare function DateTimeRangePicker({ value, onChange, className, }: DateTimeRangePickerProps): react_jsx_runtime.JSX.Element;

export { DateTimePicker, type DateTimePickerProps, type DateTimeRange, DateTimeRangePicker, type DateTimeRangePickerProps, TimeInput, type TimeInputProps };
