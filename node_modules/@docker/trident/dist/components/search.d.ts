import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';

type SearchProps = Omit<React.ComponentProps<'input'>, 'onChange' | 'type' | 'size'> & {
    /** Called with the new string value on every keystroke */
    onChange?: (value: string) => void;
    /** Called when the clear button is clicked */
    onClear?: () => void;
    /** Show loading spinner on the right */
    loading?: boolean;
};
declare function Search({ className, placeholder, value, defaultValue, disabled, loading, onChange, onClear, ...props }: SearchProps): react_jsx_runtime.JSX.Element;

export { Search, type SearchProps };
