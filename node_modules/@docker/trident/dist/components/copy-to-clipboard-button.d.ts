import * as react_jsx_runtime from 'react/jsx-runtime';
import { Button } from './ui/button.js';
import * as React from 'react';
import 'class-variance-authority/types';
import '../lib/types.js';
import 'class-variance-authority';

type CopyToClipboardButtonProps = Omit<React.ComponentProps<typeof Button>, 'onClick' | 'children' | 'aria-label'> & {
    /** Text to copy to the clipboard */
    text: string;
    /** Show "Copy" / "Copied!" text label. @default false */
    showLabel?: boolean;
    /** Callback fired after successful copy */
    onCopy?: () => void;
};
declare function CopyToClipboardButton({ text, showLabel, onCopy, className, size, ...props }: CopyToClipboardButtonProps): react_jsx_runtime.JSX.Element;

export { CopyToClipboardButton, type CopyToClipboardButtonProps };
