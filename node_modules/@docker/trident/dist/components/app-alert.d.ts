import * as react_jsx_runtime from 'react/jsx-runtime';
import { PropsWithChildren, ReactNode } from 'react';

type AppAlertProps = PropsWithChildren<{
    title?: ReactNode;
    variant?: 'default' | 'destructive';
    className?: string;
}>;
/**
 * Full-width system-level alert banner.
 * Place inside AppLayout before the sidebar/content row.
 */
declare const AppAlert: ({ children, title, variant, className, }: AppAlertProps) => react_jsx_runtime.JSX.Element;

export { AppAlert };
