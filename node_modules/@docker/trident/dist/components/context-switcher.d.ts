import * as react_jsx_runtime from 'react/jsx-runtime';

type ContextItem = {
    id: string;
    label: string;
    type: 'user' | 'org';
    avatarUrl?: string;
};
type ContextSwitcherProps = {
    items: ContextItem[];
    selected: ContextItem | undefined;
    onSelect: (item: ContextItem) => void;
    placeholder?: string;
    className?: string;
};
/**
 * Organisation / namespace context switcher.
 * Shows a searchable Command popover listing all available contexts.
 */
declare const ContextSwitcher: ({ items, selected, onSelect, placeholder, className, }: ContextSwitcherProps) => react_jsx_runtime.JSX.Element;

export { type ContextItem, ContextSwitcher };
