import * as react_jsx_runtime from 'react/jsx-runtime';
import { AsChild } from '../lib/types.js';
import { ReactNode } from 'react';

type DigestProps = {
    /** Full 64-character hex digest, without "sha256:" prefix */
    fullDigest: string;
    /** Truncate to maxLength characters. @default true */
    abbrev?: boolean;
    /** Max characters to show when abbreviated. @default 12 */
    maxLength?: number;
    /** Show copy-to-clipboard button. @default true */
    copyable?: boolean;
    tooltipPlacement?: 'top' | 'bottom' | 'left' | 'right';
    className?: string;
    /** Child element to render when `asChild` is true. */
    children?: ReactNode;
} & AsChild;
declare function Digest({ fullDigest, abbrev, maxLength, copyable, tooltipPlacement, className, asChild, children, }: DigestProps): react_jsx_runtime.JSX.Element;

export { Digest, type DigestProps };
