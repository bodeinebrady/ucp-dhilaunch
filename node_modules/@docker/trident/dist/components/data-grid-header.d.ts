import * as react_jsx_runtime from 'react/jsx-runtime';
import * as React from 'react';
import { Row, Table, ColumnOrderState } from '@tanstack/react-table';
import { FilterModel, ColumnFilter } from './data-grid-types.js';

type DataGridHeaderProps<TRow extends object> = {
    table: Table<TRow>;
    hClass: string;
    disableColumnMenu: boolean;
    disableColumnResize: boolean;
    disableColumnReorder: boolean;
    disableColumnFilter: boolean;
    dragId: React.MutableRefObject<string | null>;
    setColumnOrder: React.Dispatch<React.SetStateAction<ColumnOrderState>>;
    filterModel: FilterModel;
    onFilterChange: (filter: ColumnFilter | null, columnId: string) => void;
};
declare function DataGridHeader<TRow extends object>({ table, hClass, disableColumnMenu, disableColumnResize, disableColumnReorder, disableColumnFilter, dragId, setColumnOrder, filterModel, onFilterChange, }: DataGridHeaderProps<TRow>): react_jsx_runtime.JSX.Element;
type DataGridGroupRowProps<TRow extends object> = {
    row: Row<TRow>;
    cClass: string;
};
declare function DataGridGroupRow<TRow extends object>({ row, cClass, }: DataGridGroupRowProps<TRow>): react_jsx_runtime.JSX.Element;

export { DataGridGroupRow, DataGridHeader };
