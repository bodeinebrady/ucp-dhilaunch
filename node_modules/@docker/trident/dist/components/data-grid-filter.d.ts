import * as react_jsx_runtime from 'react/jsx-runtime';
import { FilterModel, ColumnFilter, FilterOperator, DataGridColumn } from './data-grid-types.js';
import 'react';

declare const OPERATOR_LABELS: Record<FilterOperator, string>;
declare const STRING_OPERATORS: FilterOperator[];
declare const NUMBER_OPERATORS: FilterOperator[];
/**
 * Applies a FilterModel to an array of rows client-side.
 * Uses `accessorKey` / `field` to resolve the cell value per column.
 */
declare function applyFilters<T extends object>(rows: T[], filters: FilterModel, columns: DataGridColumn<T>[]): T[];
type DataGridFilterButtonProps = {
    columnId: string;
    filterModel: FilterModel;
    onFilterChange: (filter: ColumnFilter | null) => void;
    operators?: FilterOperator[];
};
declare const DataGridFilterButton: ({ columnId, filterModel, onFilterChange, operators, }: DataGridFilterButtonProps) => react_jsx_runtime.JSX.Element;

export { DataGridFilterButton, type DataGridFilterButtonProps, NUMBER_OPERATORS, OPERATOR_LABELS, STRING_OPERATORS, applyFilters };
