import * as react_jsx_runtime from 'react/jsx-runtime';
import { SVGProps } from 'react';

/**
 * Docker logo with brand colors applied automatically.
 *
 * Uses `text-primary` (Docker blue) in all modes — the semantic token
 * switches automatically between light and dark values.
 * Size can be customized via `className` (e.g. `className="size-16"`).
 *
 * ### Component Props
 *
 * | Prop        | Type     | Default |
 * | :---------- | :------- | :------ |
 * | _className_ | `string` | —       |
 *
 *
 * ### Example Usage
 *
 * ```tsx
 * <BrandedDockerIcon />
 * <BrandedDockerIcon className="size-16" />
 * ```
 */
declare function BrandedDockerIcon({ className, ...props }: SVGProps<SVGSVGElement>): react_jsx_runtime.JSX.Element;

export { BrandedDockerIcon };
