import {
  cn
} from "../chunk-UEVWMCT6.js";

// src/components/agent-progress-summary.tsx
import { CheckCircle2Icon } from "lucide-react";
import * as React from "react";
import { jsx, jsxs } from "react/jsx-runtime";
function formatDuration(ms) {
  if (ms >= 1e3) {
    return `${(ms / 1e3).toFixed(1)}s`;
  }
  return `${Math.round(ms)}ms`;
}
function AgentProgressSummary({
  duration,
  items,
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxs(
    "div",
    {
      "data-slot": "progress-summary",
      className: cn(
        "flex items-center gap-1.5 font-mono text-[11px] text-muted-foreground",
        className
      ),
      ...props,
      children: [
        /* @__PURE__ */ jsx(
          CheckCircle2Icon,
          {
            "aria-hidden": "true",
            className: "size-3 shrink-0 text-success"
          }
        ),
        /* @__PURE__ */ jsx("span", { children: formatDuration(duration) }),
        items.map((item) => /* @__PURE__ */ jsxs(React.Fragment, { children: [
          /* @__PURE__ */ jsx("span", { "aria-hidden": "true", children: "\xB7" }),
          /* @__PURE__ */ jsxs("span", { children: [
            item.count,
            " ",
            item.label
          ] })
        ] }, item.label))
      ]
    }
  );
}
export {
  AgentProgressSummary
};
