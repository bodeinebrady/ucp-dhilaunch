export type AsChild = {
  /**
   * Use the asChild prop to compose Radix's functionality onto alternative element types or your own React components.
   *
   * See [Radix Docs](https://www.radix-ui.com/primitives/docs/guides/composition)
   *
   * @default false
   */
  asChild?: boolean;
};
